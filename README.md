CKEditor 5 classic editor + mathtype build 
========================================

## Quick start

First, install the build from npm:

```bash
npm install --save ckeditor5-mathtype-with-extra-plugins
```

And use it in your website:

`